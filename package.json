{
  "name": "pulse-os-pro-exchange",
  "version": "3.0.0",
  "private": true,
  "description": "Pulse OS - live crypto exchange-style dashboard for Netlify",
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node scripts/check.js && npm run build",
    "dev": "netlify dev"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {}
}
